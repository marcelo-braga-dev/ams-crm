import Dialog from '@mui/material/Dialog';

const PedidoCreateDialog = ({ openDialog }) => {
    return (
        <Dialog
            fullWidth
            maxWidth="lg"
            open={openDialog}
        >

        </Dialog>

    );
};
